name: Packer build pipeline
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: bash ${GITHUB_WORKSPACE}/install_packer.sh
      - run: bash cd ${GITHUB_WORKSPACE}/wsl && packer build Ubuntu-2404.pkr.hcl