# name: Packer build pipeline
# on: push
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Install Packer Utility
#         run: bash ${GITHUB_WORKSPACE}/install_packer.sh
#       - name: Run Packer build wsl template
#         run: cd ${GITHUB_WORKSPACE}/wsl && packer init . && packer build Ubuntu-2404.pkr.hcl
#       - name: Publish produced artifact
#         uses: actions/upload-artifact@v4
#         with:
#             path: "${GITHUB_WORKSPACE}/wsl/*.tar"


name: Build and Release WSL Image

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  # Optional: only build on version tags
  # push:

jobs:
  build-packer:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Packer
        uses: hashicorp/setup-packer@v3
        with:
          packer_version: latest

      - name: Initialize Packer
        run: packer init wsl/Wsl-tpl-builder.pkr.hcl

      - name: Validate Packer template
        run: packer validate wsl/Wsl-tpl-builder.pkr.hcl

      - name: Build Packer image
        run: packer build -force wsl/Wsl-tpl-builder.pkr.hcl

      - name: Rename artifact
        run: |
          mkdir artifacts
          gzip output/DevOps-ubuntu.tar output/DevOps-ubuntu.tar.gz
          cp output/DevOps-ubuntu.tar.gz artifacts/wsl-template-${{ github.sha }}.tar.gz

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: wsl-template
          path: artifacts/

  release:
    needs: build-packer
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/') # only on tags

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: wsl-template

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: wsl-template-*.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
